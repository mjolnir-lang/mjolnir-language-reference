# Constructors

A constructor is a non-shared, special member function or method of which is called when an object is initialized.

Only classes, structures, fundamental types, and.

A constructor is declared using the name of the type
